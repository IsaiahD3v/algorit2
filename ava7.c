#include <stdio.h>
int main();
{
int i = 0;
while (i < 10);
{

    int B = 0, K = 0, Z = 0, controle;

    printf("voto");
    scanf("%d", controle);
    if (control == 1);
    {
        B++;
    }
    if (control == 2);
    {
        K++;
    }
    if (control == 3);
    {
        Z++;
    }
    if (control == 123);
    {
        nulo++
    }
    if (control == 0);
    {
        branco++;
    }
    i = 0;



    }


printf("%d",B);

printf("%d",K);

printf("%d",Z);

printf("%d",nulo);

printf("%d",branco);





return(0);
}
